{% if section.settings.page.content != blank %}
  {{ 'section-main-page.css' | asset_url | stylesheet_tag }}
  {{ 'component-accordion.css' | asset_url | stylesheet_tag }}
  {{ 'section-page-collapsible.css' | asset_url | stylesheet_tag }}

  {%- style -%}
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }

    @media screen and (min-width: 750px) {
      .section-{{ section.id }}-padding {
        padding-top: {{ section.settings.padding_top_mobile }}px;
        padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
      }
    }
    #shopify-section-{{ section.id }} .page-title {
      font-size: {{ section.settings.heading_size }}px;
      color: #414141;
      margin-bottom: 52px;
    }
    #shopify-section-{{ section.id }} .page-title:after {
      content: '';
      height: 5px;
      margin-top: 20px;
      width: 50%;
      background: #96B2A1;
      display: block;
    }
    #shopify-section-{{ section.id }} p {
      color: #8f90a6;
      font-size: 14px;
      line-height: 16px;
      padding-bottom: 18px;
      margin: 0;
    }
    #shopify-section-{{ section.id }} h4 {
      font-size: 14px;
      font-weight: 700;
    }
  {%- endstyle -%}

  <div class="page-width section-{{ section.id }}-padding">
    <div class="accordion quick-add-hidden">
      <details id="Details-{{ section.id }}">
        <summary>
          <div class="summary__title">
            <h2 class="h4 accordion__title inline-richtext">
              {{ section.settings.heading | default: section.settings.page.title | escape }}
            </h2>
          </div>
          {{- 'icon-caret.svg' | inline_asset_content -}}
        </summary>
        <div class="accordion__content rte" id="PageAccordion-{{ section.id }}">
          <h2 class="page-title{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
            {%- if section.settings.page.title != blank -%}
              {{ section.settings.page.title | escape }}
            {%- else -%}
              {{ 'sections.page.title' | t }}
            {%- endif -%}
          </h2>
          <div>
            {{ section.settings.page.content }}
          </div>
        </div>
      </details>
    </div>
  </div>
{% endif %}

{% schema %}
{
  "name": "t:sections.page_collapsible.name",
  "tag": "section",
  "class": "section section-page-collapsible",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "default": "t:sections.page_collapsible.settings.heading.default",
      "info": "t:sections.page_collapsible.settings.heading.info",
      "label": "t:sections.page_collapsible.settings.heading.label"
    },
    {
      "type": "page",
      "id": "page",
      "label": "t:sections.page_collapsible.settings.page.label"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "t:sections.all.heading_size.label",
      "step": 1,
      "min": 8,
      "max": 100,
      "default": 24
    },

    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Section padding top (Mobile)",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Section padding bottom (Mobile)",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "t:sections.page_collapsible.presets.name",
      "category": "Custom Sections"
    }
  ]
}
{% endschema %}
